# Copy this file to secrets.yaml and fill in your values
# Don't commit secrets.yaml to version control!

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

# Optional: uncomment if using encrypted API
# api_encryption_key: "YOUR_GENERATED_KEY"

# Optional: uncomment if using OTA password
# ota_password: "YOUR_OTA_PASSWORD"

# Optional: fallback AP password
# fallback_password: "YOUR_FALLBACK_PASSWORD"
